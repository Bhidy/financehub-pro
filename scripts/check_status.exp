#!/usr/bin/expect -f

set timeout 30
set host [lindex $argv 0]
set password [lindex $argv 1]

spawn ssh -o StrictHostKeyChecking=no root@$host "docker ps -a && echo '--- LOGS ---' && docker logs --tail 20 starta-backend-1"

expect "password:"
send "$password\r"

expect eof
