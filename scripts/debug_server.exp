#!/usr/bin/expect -f

set timeout 30
set host [lindex $argv 0]
set password [lindex $argv 1]

spawn ssh -o StrictHostKeyChecking=no root@$host "docker logs starta-caddy-1 --tail 100 2>&1"

expect "password:"
send "$password\r"

expect eof
