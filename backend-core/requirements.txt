# ⛔️ STRICT CPU-ONLY REQUIREMENT ⛔️
# FAILURE TO USE CPU-ONLY TORCH WILL CRASH THE SERVER (DISK EXHAUSTION)
# DO NOT REMOVE THE --extra-index-url FLAG BELOW
# =========================================================================
# Hygiene (audit 2026-06-11): file deduplicated (fastapi/uvicorn/aiohttp/
# requests/beautifulsoup4/python-dotenv/pydantic each appeared TWICE) and the
# previously version-less packages now carry an upper MAJOR bound (except
# torch/stripe/lxml/yfinance-family, deliberately floor-only — their major
# cadence is unpredictable and over-tight caps break rebuilds) so a surprise
# major release can never silently change what a rebuild installs. Exact pins are
# unchanged from what production runs. When bumping: change the bound here,
# deploy via the "Backend Deploy" workflow, watch the health check.
# =========================================================================

# --- Web framework / API ---
fastapi==0.109.0
uvicorn==0.27.0
pydantic>=2.0,<3
pydantic-settings>=2.0,<3
python-multipart>=0.0.6,<1
email-validator>=2.0,<3

# --- Auth ---
python-jose[cryptography]>=3.3,<4
passlib[bcrypt]>=1.7,<2

# --- Database ---
sqlalchemy>=2.0,<3
asyncpg>=0.29,<1
psycopg2-binary>=2.9,<3
databases>=0.8,<1

# --- HTTP / scraping ---
aiohttp==3.9.1
requests==2.31.0
httpx>=0.25.0
beautifulsoup4==4.12.2
lxml>=4.9
tls-client==1.0.1
curl-cffi==0.5.10
scrapling==0.4
playwright>=1.40.0
cffi==1.16.0

# --- LLM providers ---
openai>=1.0,<3
groq>=0.9,<2

# --- Data / scheduling ---
pandas>=2.0,<3
numpy>=1.26,<3
yfinance>=0.2.50
yahooquery>=2.3.0
openpyxl>=3.1,<5
APScheduler>=3.10,<4
schedule==1.2.1
cachetools>=5.3,<7

# --- Payments / email / misc ---
stripe>=7
aiosmtplib>=2.0,<5
python-dotenv>=1.0,<2
colorama==0.4.6
pyyaml>=6.0

# Optimized for Hetzner VPS (CPU Only)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
